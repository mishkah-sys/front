# Mishkah System Guide: DSL & UI - الدليل الشامل

> **"Build beautiful software without the chaos."**

هذه الوثيقة هي مرجعك الأساسي للكتابة بنظام **Mishkah DSL** واستخدام مكتبة **Mishkah UI** القياسية.

---

## 🏗️ طرق الكتابة (DSLs)

مشكاة توفر لك حرية اختيار الأسلوب الأنسب لفريقك ومشروعك. لدينا الآن **3 طرق معتمدة**:

### 1. الكتابة المباشرة (Direct DSL) - *الأسرع والأحدث* 🚀
h(tag, category, config, children)

// مثال كامل:
const item = h('div', 'Containers', { attrs: { class: 'item' } }, [
  h('span', 'Text', {}, ['Item Name'])
]);

// ✅ الطريقة الأكثر تفضيلاً: gkey في المستوى الأعلى (Top-level)
h('button', null, { 
  attrs: { class: 'btn' }, 
  gkey: 'action:save' 
}, ['Save']);

// هيكل الـ config الصارم:
/*
  config = {
    attrs: { class: 'btn', id: 'save' }, // صفات (بيانات فقط)
    events: { ... }, // (نادراً ما تستخدم مباشرة)
    gkey: 'save_action', // مفتاح السلوك (أولوية قصوى)
    key: 'unique_id' // مفتاح القوائم
  }
*/
```

### 3. التصنيفات (Categorized DSL) - *الكلاسيكية* 🏛️
الطريقة التقليدية المنظمة، ما زالت مدعومة بالكامل.
```javascript
D.Containers.Div(...)
D.Forms.Button(...)
D.Text.P(...)
```

---

## 🆚 مقارنة عميقة: Mishkah vs React

### 1. الفلسفة: Atoms vs Components

**في React:**
أنت تفكر بـ **"Components" ذكية**. الزر يعرف ماذا يفعل عند الضغط عليه. يتم تمرير الدوال (Callbacks) إلى داخل المكون.

**في Mishkah:**
أنت تفكر بـ **"Atoms" (ذرات) غبية ومطيعة**. الزر (Button) لا يعرف ماذا سيحدث. هو فقط يصرخ *"أنا ضُغطت!"* ويرسل مفتاحه (`gkey`). المتحكم المركزي (Controller/App) هو من يقرر ماذا يفعل.
> **الفائدة:** هذا يجعل إعادة استخدام المكونات أسهل بكثير، لأنك لا تحتاج لتعديل كود الزر لتغيير وظيفته، بل تغير الـ Order فقط.

### 2. الذكاء الهجين (Hybrid Intelligence) 🧠✨

لأننا نعلم أن الـ AI (ومبرمجي React) معتادون على نمط `(tag, props, children)`، قمنا بتحديث دالة `h` لتكون "ذكية" وتقبل الصيغتين!

إذا كتبت الكود بصيغة React:
```javascript
// ✅ ستقبلها مشكاة وتحولها داخلياً للصيغة الصحيحة!
h('div', { className: 'card', id: 'my-card', onClick: '...' }, [
  h('span', {}, ['Text'])
])
```

سيقوم النظام تلقائياً بـ:
1. تحويل `className` ← `class` (داخل `attrs`).
2. نقل `id`, `style`... ← `attrs`.
3. نقل `onClick`... ← `events`.
4. تعيين التصنيف الافتراضي `Containers`.

**لماذا؟**
لكي لا "يتلخبط" الـ AI، ولكي يكون الانتقال من React لمشكاة سلساً للجميع. نحن نأخذ أفضل ما في العالمين: **سهولة الكتابة (DX)** مع **صرامة النظام (Architecture)**.

### 3. جدول المقارنة التفصيلي

| وجه المقارنة | React `createElement` | Mishkah `h` | الفائدة في مشكاة |
| :--- | :--- | :--- | :--- |
| **الأبناء** | Rest Arguments `...` | Array `[]` | **أداء أعلى** (V8 Friendly) |
| **البيانات** | مخلوطة (Props + Functions) | مفصولة (`attrs` نقية) | **Serializability** (يمكن نسخها/حفظها) |
| **السلوك** | جزء من المكون (Coupled) | مفصول تماماً (`gkey`) | **أمان** وصيانة أسهل |
| **النظام** | حرية مطلقة (Chaos) | موجه ومقيد (Order) | **كود موحد** لكامل الفريق |

---

## 🎨 Mishkah UI Standard Library

مشكاة لا تأتي فارغة! لدينا مكتبة **UI** غنية ومبنية مسبقًا تعتمد على **Tailwind Tokens** وتدعم **الوضع الليلي (Dark Mode)** و **RTL** تلقائياً.

> **نصيحة ذهبية:** قبل أن تبني أي عنصر، ابحث عنه في `UI`. أعدنا لك بناء الأساسيات لتوفر وقتك.

### 🔍 كيف أرى جميع المكونات؟
افتح الـ Console في المتصفح واكتب:
```javascript
M.help.components()
```
سيعرض لك قائمة بجميع الـ UI المتاحة. ولتفاصيل عن مكون محدد:
```javascript
M.help.component('Modal')
```

### 💎 أهم المكونات الجاهزة (Core Components)

هذه القائمة موجودة في `mishkah-ui.js` ويمكنك استخدامها فوراً عبر `Mishkah.UI` أو `UI`:

#### 1. الأساسيات (Basics)
- **`UI.Button`**: زر قياسي يدعم variants (solid, soft, ghost, destructive) وأحجام (sm, md, lg).
- **`UI.Input`**: حقل إدخال مصمم بجماليات الـ System.
- **`UI.Label`**: تسميات الحقول.
- **`UI.Badge` / `UI.Chip` / `UI.Pill`**: لعرض الحالات والتصنيفات.
- **`UI.Divider`**: خط فاصل أنيق.

#### 2. الهياكل (Structures)
- **`UI.Card`**: بطاقة جاهزة مع Header, Content, Footer.
- **`UI.AppRoot`**: الحاوية الرئيسية للتطبيق (Shell).
- **`UI.Toolbar`**: شريط أدوات علوي (Sticky).
- **`UI.Footerbar`**: شريط سفلي.
- **`UI.HStack` / `UI.VStack`**: حاويات Flex جاهزة لترتيب العناصر أفقياً وعمودياً.

#### 3. القوائم والعرض (Display)
- **`UI.List`**: قائمة عناصر منسقة.
- **`UI.Table`** (و `UI.Datatable` إن وجد): جداول بيانات.
- **`UI.Empty`**: واجهة "لا توجد بيانات" جاهزة.
- **`UI.Stat.Card`**: بطاقة لعرض الإحصائيات والأرقام.

#### 4. النوافذ والتراكب (Overlays)
- **`UI.Modal`**: نافذة منبثقة (Dialog) تدعم أحجام مختلفة (sm, md, lg, full).
- **`UI.Drawer`**: قائمة جانبية منزلقة.
- **`UI.Toast`**: إشعارات عائمة.

#### 5. الرسوم البيانية (Charts)
- **`UI.Chart.Canvas`**: حاوية Chart.js جاهزة ومتجاوبة.

---

## 📝 فلسفة العمل والكتابة (The Mishkah Way)

لكي تكتب كوداً "مشكاوياً" أصيلاً، اتبع هذه القواعد المستمدة من **الأركان السبعة**:

### 1. افصل بين البناء والسلوك (Structure vs Behavior)
في الـ DSL، نحن نبني الهيكل فقط. لا نكتب دوال داخل الـ UI.
- **خطأ ❌**: `onclick: () => alert('hi')`
- **صح ✅**: `gkey: 'feature:action'` ثم نعالجها في `orders`.

### 2. استخدم `t()` للترجمة (i18n)
مشكاة تدعم تعدد اللغات من النواة. لا تكتب نصوصاً ثابتة.
```javascript
// الخيار الأفضل: استخدم مفتاح الترجمة مباشرة
D.H1({ t: 'welcome_message' })

// أو دالة t() داخل النصوص المركبة
D.P({}, [ t(db, 'hello') + ' ' + userName ])
```

### 3. التصميم بالرموز (Design Tokens & Tailwind)
نحن نستخدم `mishkah-utils` لدعم Tailwind.
- استخدم `UI.*` قدر الإمكان لأنها تستخدم الـ Tokens القياسية (`var(--primary)`, `var(--radius)`).
- عند الحاجة لتنسيق خاص، استخدم `attrs: { class: tw'...' }`.

### 4. إعادة الاستخدام (Reusability)
إذا صنعت زرًا أو بطاقة جميلة، **لا تتركها في كود الصفحة**.
1. انقلها إلى ملف منفصل أو أضفها لـ `mishkah-ui.js` إذا كانت عامة.
2. اجعلها تستقبل `config` و `children`.
3. وثقها لزملائك.

---

## 🛠️ أدوات المطور (DevTools)

لمساعدتك أثناء العمل، توفر مشكاة أدوات في الـ Console:

- **`M.help()`**: القائمة الرئيسية للمساعدة.
- **`M.help.inspect(element)`**: مرر له أي عنصر DOM لتعرف الـ gkey والبيانات المرتبطة به.
- **`M.help.components()`**: لعرض مكتبة الـ UI.
- **`M.help.templates()`**: لنسخ قوالب كود جاهزة.

---

> **خلاصة:**
> ابدأ بـ **Direct DSL** (`D.Div`) للسرعة. استعن بـ **`UI.*`** للجمال والتوحيد. التزم بـ **`gkey`** للنظام. واستخدم **`M.help`** كلما ضعت.
